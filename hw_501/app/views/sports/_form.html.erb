<%= form_for(@sport) do |f| %>
  <% if @sport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sport.errors.count, "error") %> prohibited this sport from being saved:</h2>

      <ul>
      <% @sport.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :favorite_team %><br>
    <%= f.text_field :favorite_team %>
  </div>
  <div class="field">
    <%= f.label :favorite_player %><br>
    <%= f.text_field :favorite_player %>
  </div>
  <div class="field">
    <%= f.label :why %><br>
    <%= f.text_area :why %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
